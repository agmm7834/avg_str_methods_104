## **1. upper() – 8 ta**

1. Berilgan `"hello python 3.12"` satrida faqat harflarni `upper()` qilib, raqam va bo'sh joylarni o'zgartirmasdan chiqaruvchi ifoda yozing.
2. Foydalanuvchi kiritgan jumladagi barcha **brend nomlarini** (`samsung, apple, xiaomi`) upper shaklida ajratib oling.
3. `"my file name.Py"` yozuvidagi `.Py` kengaytmasini to'liq upper holatiga o'tkazing.
4. Parol tekshirish: agar parol faqat `upper()` holatida kirilsa, uni rad eting.
5. Matnda uchragan `"id:"` qismlarini avtomatik ravishda `"ID:"` ga aylantiring.
6. `"firstName: john"` satridagi qiymatni (`john`) upper shaklga keltiring.
7. `"login: admin; role: superUser"` satridan `role` qiymatini ajratib olib, uni upper qiling.
8. Ko'p qatorli matndan faqat bosh harf bilan boshlanmagan gaplarni topib, ularni upper shaklida qaytaring.

---

## **2. lower() – 8 ta**

9. Foydalanuvchi yozgan matndan faqat **email manzillarini** topib, `lower()` formatiga keltiring.
10. `"HeLLo WoRLD PyTHon"` matnida faqat `WORLD` so'zini lower qiling.
11. Parolda **katta harf** ishlatilgan bo'lsa, uni `lower()` ga o'tkazib qayta tekshiring.
12. `"FileName.JPEG"` ni kengaytmasi bo'yicha lower formatga keltiring.
13. Matndagi barcha `"HTTP", "HTTPS"` qismlarini lower formatga o'tkazing.
14. `"UserName: GULNOZA"` satridagi qiymatni lower formatda ajrating.
15. `"PAYMENT STATUS: PAID"` satridan `"PAID"` qiymatini lower shaklda qaytaring.
16. Qator oxiridagi `"OK"` yoki `"ERROR"` so'zlarini lower formatga o'tkazing.

---

## **3. title() – 8 ta**

17. `"python web development for beginners"` kurs nomini `title()` formatida chiqaring.
18. `"introduction to artificial intelligence"` — kitob nomini title shaklga keltiring, biroq `to`, `in`, `for` so'zlari kichik harfda qolsin.
19. `"gulnoza shukurullayeva"` F.I.O ni title shaklga o'tkazing.
20. Matndan barcha film nomlarini topib, title formatga keltiring.
21. `"full-stack web developer"` kasbini title shakliga keltiring.
22. `"the rise and fall of python"` matnini title qiling, lekin `"of"` so'zi kapslock bo'lmasin.
23. `"welcome-to-the-jungle"` matnini `-` bo'yicha bo'lib, har bir qismini title qiluvchi dastur yozing.
24. `"my_new_project_name"` ni `_` bo'yicha ajratib, title qilib birlashtiring.

---

## **4. capitalize() – 8 ta**

25. `"hELLO wORLD"` matnini faqat birinchi harfi katta bo'ladigan holatga keltiring.
26. `"python STRING methods"` ni capitalize qilib, qolganini lower qiling.
27. `"onLayn magazin"` yozuvini grammatik tarzda capitalize qiling.
28. `"   welcome to python"` bosh bo'sh joylarini olib tashlab capitalize qiling.
29. `"ERROR: invalid password"` matnida `"invalid password"` qismini capitalize qiling.
30. `"new message FROM ADMIN"` — faqat `"new message"` qismini capitalize formatga o'tkazing.
31. `"data: PYTHON PROGRAMMING"` dan `data:` dan keyingi qismni capitalize qiling.
32. Matndagi har bir yangi gapni (`.` dan keyin) capitalize yordamida to'g'rilang.

---

## **5. strip() – 8 ta**

33. `"---hello--"` dan **faqat boshidagi** 3 ta `-` belgini oling.
34. Seriya raqami `"***A12-B34***"` dan yulduzcha va bo'sh joylarni tozalang.
35. `"   id: 123   "` matnini tozalab, faqat `"id: 123"` qoldiring.
36. `"____python____"` matnidan `strip('_')` bilan asosiy so'zni ajrating.
37. `"|||user|||role|||"` matnidan `user` bo'limini ajrating.
38. `"==   test code   =="` dan avval `strip('=')`, keyin `strip()` qiling.
39. `"000123000"` dan raqamlarni o'zgartirmasdan faqat `"0"` larni tozalang.
40. `"<html>content</html>"` dagi `<` `>` belgilarini tozalang.

---

## **6. lstrip() – 8 ta**

41. `"0000123"` matndan chap tarafdagi barcha `"0"` larni olib tashlang.
42. `"   ---hello"` dan chap bo'sh joylar va `-` larni tozalang.
43. `"###Section1###"` dan chap tomondagi `#` belgilarini ajrating.
44. `"....loading"` dan boshidagi nuqtalarni olib tashlang.
45. `"////path/to/file"` ni `lstrip('/')` bilan normallashtiring.
46. `"     Gulnoza"` dan chap bo'sh joylarni oling.
47. `"___value=42"` dan chapdagi chiziqlarni olib tashlang.
48. `"&&&login=admin"` chap tomonlardagi `&` larni oling.

---

## **7. rstrip() – 8 ta**

49. `"hello000"` dan oxirdagi 0 larni tozalang.
50. `"python>>>   "` dan oxirgi `>` va bo'sh joylar tozalansin.
51. `"file.txt....."` dan nuqtalarni olib tashlang.
52. `"value=123;;;;"` dan `;` belgilarini oxiridan olib tashlang.
53. `"note!!!"` matnida nechta `!` borligini hisoblab, oxiridagini tozalang.
54. `"message___"` dan oxirdagi `_` larni olib tashlang.
55. `"123000000"` dan oxirgi 0 larni tozalang.
56. `"hello world     "` matnini rstrip qiling.

---

## **8. replace() – 8 ta**

57. `"user_id=123&status=active"` dagi `&` ni yangi qator (`\n`) bilan almashtiring.
58. `"aabbcc1122"` matnidan ketma-ket kelgan `"aa"`, `"bb"`, `"cc"` ni `"*"` ga o'zgartiring.
59. `"text with    multiple spaces"` dagi ortiqcha bo'sh joylarni `" "` ga kamaytiring.
60. `"John_Doe-2025"` dagi `_` ni `" "`, `-` ni `/` bilan almashtiring.
61. Paroldan barcha `" "` (space) larni olib tashlab, `"*"` bilan almashtiring.
62. `"hello_world_python"` ni bosqichma-bosqich `" "` bilan almashtirib, butun jumlaga aylantiring.
63. `"URL: https://site.com/page?id=5"` dagi `https://` qismini `"http://"` ga almashtiring.
64. `"a1b2c3d4"` matnida har bir harfdan keyingi raqamni `"*"` bilan o'zgartiring.

---

## **9. split() – 8 ta**

65. `"name=john;age=25;role=admin"` ni `;` bo'yicha ajrating va key-value ko'rinishiga aylantiring.
66. `"12-34-56-78"` matnini `split('-')` bilan ajrating va juftliklar shaklida chiqaring.
67. `"python|string|methods"` matnini bo'lib, 3 ta elementli ro'yxat hosil qiling.
68. `"john   doe   developer"` ni bo'sh joy bo'yicha split qilib, ortiqcha bo'sh joylarni inkor eting.
69. `"path/to/my/file.txt"` yo'lini `split('/')` qilib bo'ling.
70. `"a=b=c=d"` matnini `=` bo'yicha ajratib oxirgi qiymatni oling.
71. `"2025/12/13"` sanasini yil, oy, kun ko'rinishida ajrating.
72. `"#python#java#c++"` dan bo'sh bo'lmagan elementlarni ajratib oling.

---

## **10. join() – 8 ta**

73. Ro'yxat `["john","doe","developer"]` ni `" | "` bilan join qiling.
74. `"python"` harflarini `"-"` bilan join qilib sanash tizimi yarating.
75. `"123456"` belgilarini `","` bilan ajratib join qiling.
76. Matndan olingan 3 ta so'zni `"***"` bilan birlashtirib qaytaring.
77. `["UZ","RU","EN"]` ro'yxatini `" / "` bilan join qiling.
78. `"hello world"` jumlasidagi so'zlarni `join()` orqali bog'lab qayta yozing.
79. Foydalanuvchi ismi, familiyasi va yoshini `" | "` bilan birlashtiring.
80. `"abcdef"` belgilarini `":"` bilan bog'langan ko'rinishga o'tkazing.

---

## **11. find() – 8 ta**

81. `"hello python world"` matnida `"python"` substringining indeksini toping.
82. `"id=123-user=admin"` da `"user"` qayerdan boshlanishini aniqlang.
83. `"abcabcabcabc"` matnda `"abc"` ning **ikkinchi uchrashuvi** indeksini toping.
84. `"welcome_home"` da `"home"` boshlanish indeksini toping.
85. `"token: 45fj9k3"` dan `"fj9"` substring indeksini toping.
86. `"hello-world-python"` da `-` belgilarining joylashuvlarini toping.
87. `"https://example.com"` da `"//"` qayerdan boshlanishini aniqlang.
88. `"a1b2c3d4"` matnida birinchi raqam qayerda boshlanishini toping.

---

## **12. count() – 8 ta**

89. `"abracadabra"` da `a` nechta borligini toping.
90. `"hello python hello world"` da `"hello"` so'zi nechta?
91. `"xxxyyyzzzxxyy"` da `xx` nechta uchraydi?
92. `"1122334455"` da `"3"` nechta bor?
93. `"banana"` da `"na"` substring nechta borligini aniqlang.
94. `"python-programming-language"` da `-` lar sonini sanang.
95. `"UPPERlowerUPPERlower"` da `"upper"` nechta ekanini lower qilib sanang.
96. `"test test:test;test"` matnida `"test"` necha marotaba uchraydi?

---

## **13. startswith() / endswith() – 8 ta**

97. `"report_2025_final.pdf"` `.pdf` bilan tugaydimi?
98. `"https://github.com"` `https://` bilan boshlanishini tekshiring.
99. `"image_001.png"` `.jpg` bilan tugasa ham, `.png` bilan tugashini aniqlang.
100. `"admin:root"` `"admin"` bilan boshlanishini tekshiring.
101. `"000123"` `"0"` lar bilan boshlanishini aniqlang va nechta ekanini sanang.
102. `"data_backup_01.zip"` `.zip` bilan tugashini tekshiring.
103. `"hello_world.py"` `.py` bilan tugasa `YES`, aks holda `NO` chiqaring.
104. `"superuser"` `"super"` bilan boshlanishini tekshiring va davomidagi qismni ajrating.

---
